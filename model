from tensorflow.keras.models import load_model

# Cargar el modelo .h5
model_path = "BrainTumorDetection.h5"  # Asegúrate de colocar la ruta correcta
model = load_model(model_path)

# Mostrar resumen del modelo
model.summary()
